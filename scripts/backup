#!/bin/bash
source _common.sh
source /usr/share/yunohost/helpers

# IMPORTANT: Stop containers before backing up data directories.
# Copying files from a running database (ElasticSearch, CockroachDB, etc.) can produce
# a corrupted backup. This causes brief downtime but ensures data integrity.
ynh_systemctl --service="$app" --action="stop" --log_path="/var/log/$app/$app.log"
sleep 5 # ensure they're dead

ynh_backup "$install_dir"
ynh_backup "$data_dir"
ynh_backup "/etc/nginx/conf.d/$domain.d/$app.conf"
ynh_backup "/etc/systemd/system/$app.service"
ynh_backup "/var/log/$app"

# Restart after backup
ynh_systemctl --service="$app" --action="start" --log_path="/var/log/$app/$app.log"
